// import { ripgrepMatch } from './utils'

describe('utils', () => {
	// describe('ripgrepMatch', () => {
	// 	it('converts a line with one match in the middle', () => {
	// 		const match = ripgrepMatch({
	// 			line: 'this is a RG_RESULT of ripgrep',
	// 			originalLine: 'this is a test of ripgrep'
	// 		})
	// 		expect(match).toEqual([10, 13])
	// 	})
	// })
})
